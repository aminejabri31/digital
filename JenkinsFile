pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Build your Spring project using Maven
                bat 'mvn clean package'
            }
        }
        
        stage('Test') {
            steps {
                // Run tests using Maven
                bat 'mvn test'
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                // Run SonarScanner analysis
                withSonarQubeEnv('sq2') {
                    bat '
                        mvn clean verify sonar:sonar 
                        -Dsonar.projectKey=sonar1 
                        -Dsonar.projectName='sonar1' 
                        -Dsonar.host.url=http://localhost:9000 
                        -Dsonar.token=sqp_92240ddac508a50d9fdfff5bf7f883f44bd9513f
                    '
                }
            }
        }
        
        stage('Deploy') {
            steps {
                // Deployment steps (e.g., deploying to a server)
                // This may vary based on your deployment strategy
                // Example: deploying to Tomcat
                echo "Deployed"
            }
        }
    }
}
